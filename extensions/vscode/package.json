{
  "name": "context-to-agent",
  "displayName": "ContextToAgent",
  "description": "ContextToAgent - a lightweight MCP bridge that shares the current editor context with desktop agents. (Chinese: 将当前编辑器上下文以 MCP 方式共享给桌面 Agent 的轻量级桥接工具。)",
  "version": "0.1.15",
  "publisher": "local",
  "license": "Apache-2.0",
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:contextToAgent.openSettings",
    "onCommand:contextToAgent.configureAgents",
    "onCommand:contextToAgent.openDashboard"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "contextToAgent.openSettings",
        "title": "ContextToAgent: Open VS Code Settings"
      },
      {
        "command": "contextToAgent.configureAgents",
        "title": "ContextToAgent: Open Dashboard"
      },
      {
        "command": "contextToAgent.openDashboard",
        "title": "ContextToAgent: Open Dashboard"
      }
    ],
    "configuration": {
      "title": "ContextToAgent",
      "properties": {
        "contextToAgent.language": {
          "type": "string",
          "default": "auto",
          "enum": [
            "auto",
            "en",
            "zh-CN"
          ],
          "enumDescriptions": [
            "Follow the VS Code display language.",
            "Use English.",
            "使用中文。"
          ],
          "description": "Display language for the ContextToAgent dashboard."
        },
        "contextToAgent.configPaths": {
          "type": "object",
          "default": {},
          "additionalProperties": false,
          "properties": {
            "codex": {
              "type": "string",
              "default": "",
              "description": "Override the Codex config path. Leave empty to use the platform default."
            },
            "opencode": {
              "type": "string",
              "default": "",
              "description": "Override the OpenCode config path. Leave empty to use the platform default."
            },
            "claudeCode": {
              "type": "string",
              "default": "",
              "description": "Override the Claude Code CLI config path. Leave empty to use ~/.claude.json."
            },
            "claudeDesktop": {
              "type": "string",
              "default": "",
              "description": "Override the Claude Desktop config path. Useful for deployments such as Claude-3P."
            }
          },
          "description": "Optional config file path overrides for built-in agents. Paths may start with ~."
        }
      }
    }
  }
}
